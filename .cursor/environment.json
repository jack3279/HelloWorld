{
  "name": "HelloWorld",
  "install": "sudo apt-get update -qq && sudo apt-get install -y -qq python3-venv && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "web",
      "command": ".venv/bin/python app.py"
    }
  ]
}
